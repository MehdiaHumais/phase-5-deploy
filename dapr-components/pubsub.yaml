apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: kafka-pubsub
spec:
  type: pubsub.kafka
  version: v1
  metadata:
  - name: brokers
    value: "localhost:9092"  # Update for production to Redpanda Cloud
  - name: consumerGroup
    value: "todo-service"
  - name: authRequired
    value: "false"